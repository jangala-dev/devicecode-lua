-- Development device configuration

return {
    services = {
        "config",
        "hub",
        -- "system",
        "testservice"
        -- ... other services specific to this device version
    }
}
